{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Ansible preset - cap Python runtime to <3.14.0, including when managed via mise",
  "packageRules": [
    {
      "description": "Cap Python runtime to <3.14.0 when managed via mise (Ansible doesn't support 3.14 yet)",
      "matchManagers": [
        "mise"
      ],
      "matchPackageNames": [
        "python"
      ],
      "allowedVersions": "<3.14.0"
    }
  ]
}
